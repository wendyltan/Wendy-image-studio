# Wendy Image Studio — Codex 项目指令

## 使命与成功标准

本项目是本机运行的《温蒂的日常》漫画创作工作台。核心目标是可靠完成“故事/分镜方案 → 冻结参考素材和提示词 → ChatGPT 网页真实生图或编辑 → 原图保存 → 质检与本地排版 → 人工验收”。用户实际拿到正确的原图和成稿，比增加测试数量、状态字段和防御性补丁更重要。

**禁止把 `npm test` 全绿等同于真实生图可用。** 仓库现有浏览器/生图测试大量使用假 Codex worker、模拟事件、模拟 manifest 和极小图片 fixture。本地测试通过，只能报告 LOCAL REGRESSION PASS；未执行真实 Chrome→ChatGPT→上传→发送→生成→下载，就必须报告 LIVE IMAGE ACCEPTANCE: NOT RUN。

## 仓库地图

- `app/`：创作室 UI 和人工确认入口。
- `server/engine.mjs`、`server/image-workflow.mjs`：作品、任务、分镜和生图编排。
- `server/chatgpt-web-provider.mjs`：单张图片请求、冻结附件和浏览器执行结果。
- `server/web-executor-instructions.mjs`：交给子 Codex 的真实网页操作协议；这是生产代码，不是普通文案。
- `server/bridge.mjs`：Codex CLI 启动、事件与浏览器调用预算。
- `server/owned-tab-lease.mjs`、`server/run-manifest.mjs`、`server/task-state.mjs`：身份、标签页与任务状态。
- `server/web-upload-evidence.mjs`、`server/web-download-evidence.mjs`：上传/原图下载证据。
- `tests/`：主要验证本地逻辑与模拟环境，不自动代表线上验收。
- `作品/`、`.runtime/`、`.制作记录/`：可能含用户作品及真实失败记录。只读诊断，除非本次任务明确授权修改；不要将其内容或密钥提交到 Git。

## 修改前必须完成

1. 先查看 `git status --short`、最近相关 commit 和目标文件，保留用户已有未提交修改。未经授权不得 `reset --hard`、`clean -fd`、强制推送或批量删除。
2. 若任务涉及真实生图故障，以**最新一次实际运行**为准（旧审计文档和 commit 只用于对照）。先读同一次 `.制作记录/<runId>/` 的 `web-generation.json`、`execution.json` 和所需的 `events.jsonl` 阶段片段，再按首个失败阶段补读 `request.json`、`worker-request.json`、`owned-tab-lease.json`、`browser-tool-budget.json`、上传/下载证据（以实际存在的文件为准）。不要每轮重读整个仓库、全部旧 run、超长 prompt 或完整日志；先按时间先后定位首次偏离预期的位置。
3. 把结论标成 **已证实 / 高概率假设 / 尚未验证**。没有本机运行记录时，不能仅凭 commit 名称断言用户最近一次失败的根因。
4. 简要给出：复现或证据 → 第一处故障 → 最小修复范围 → 可能受影响的正常路径 → 验收方式。不要先写新状态机再寻找理由。

## 修复边界

- 一次任务只处理一个明确的失败机制；优先小补丁和既有边界，不顺便重构 UI、下载、上传、恢复及排版全链路。跨模块确有必要时说明数据或控制流理由。
- `web-executor-instructions.mjs` 新增或修改规则前，通读整个执行步骤并检查语义矛盾，尤其是 `goto` 次数、owned tab、上传次数、发送次数、等待和 cleanup；不能只对新增片段做字符串匹配测试。浏览器执行器提示词属于高 token 成本生产协议；优先精简重复、相互冲突的指令，固定确定性脚本块，避免让低思考执行器临场重写检测器。
- 处理 readiness/上传入口故障时，分别保留“本地预期脚本”和**真实 CUA 调用脚本**、可定位的解析错误，以及有限且脱敏的相关 AX 控件证据；离线校验待执行 JS 的语法、身份和一次导航/发送边界，避免仅凭本地 fixture 通过或一句 `ready=true` 放行。失败诊断不得被 cleanup 快照覆盖；旧请求的 ready 证据不得授权新请求上传。
- 不要为了“让测试绿”放宽生产断言或只改测试预期。改变测试契约须给出真实故障证据或明确的行为规范，并补上旧缺陷会失败的回归用例。
- 优先减少相互重叠的状态、魔法正则和 prompt 指令；不要每遇到一种真实异常就无期限增加错误码、恢复分支或超时预算。
- 单张任务只允许一个已归属的浏览器标签页和一次提交。**发送结果未知不自动重发**；中断时保留原图、任务身份与审计证据，不伪称已上传、已发送、已关闭或“可安全重试”。
- 保持冻结参考图的文件身份、顺序及编辑基图；修改一格不得静默污染其他格。不要把参考图缩略图、网页截图或预览冒充下载的生成原图。
- 不要隐藏日志、手工伪造 manifest/lease/成功证据，也不要用假 1×1 图片通过“真实生图”验收。
- 本仓库根部 `AGENTS.md` 约束在仓库中进行开发的 Codex。项目内部通过 CLI 从 `.制作记录/<runId>/` 启动的生图执行子进程，另有独立的生产指令；不要假设本文件可替代或自动修复那份执行协议。
- `.gitignore` 排除了 `docs/*.md` 和本地素材提案；文档可能只在当前 Mac 存在，不要假定 fresh clone 能读到排障清单，也不要用 `git add -f` 顺手提交用户作品或私有运行记录。

## 验证与报告

先跑能复现首个故障的单文件/单用例测试，再跑相关模块测试；提交前尽可能执行 `npm test`、`npm run lint`、`npm run build`，记录实际命令和退出结果；没有运行则写 NOT RUN，失败则写 FAIL。若全量测试中旧用例失败，查看断言及现行行为、独立重跑，不隐瞒也不为绿色而弱化断言。不要启动多个重复的 Codex 修复者、重复全量测试或无证据的真实生图试错。对浏览器执行链路的修复，优先用真实失败运行记录定位，再在用户授权且账号、额度和环境允许时做一次**可观察的真实单图 smoke test**：真实参考图上传完成、发送已确认、实际生成原图、文件可解码且身份与本次任务匹配。只要某一步缺失，就不能报告 LIVE IMAGE ACCEPTANCE: PASS。

真实运行会消耗额度；未经当前任务授权，不自动产生多次付费/额度生图。一次真实测试失败后，先分析同一 runId，不自动反复重试；结果未知时尤其禁止重发。

每次交付请按以下格式简洁汇报：

- 首次失败点及证据（文件/行号/runId）；区分事实和猜测。
- 修改文件及为何必须修改；注明受影响的既有成功路径。
- `LOCAL REGRESSION` / `LINT` / `BUILD` / `LIVE IMAGE ACCEPTANCE`：分别标记 PASS、FAIL 或 NOT RUN。
- 尚未验证的行为、是否可能有已发送但未保存的图，以及建议的下一个**单一**动作。

**完成定义：代码改动 + 相关本地验证 ≠ 真实生图完成。不得以“全量测试通过”“已彻底解决”等笼统语言取代分层验收。**
